# ===========================================
# Listwell API (apps/api)
# ===========================================
# Copy to .env.local and fill in values

# --- Database ---
DATABASE_URL=postgresql://listwell:listwell@localhost:5433/listwell
# Set to "neon" in production for Neon serverless driver; omit for local PostgreSQL
# DB_DRIVER=neon

# --- Authentication (BetterAuth) ---
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
# Used for BetterAuth baseURL
API_URL=http://localhost:4000
# Used for CORS and trustedOrigins
WEB_URL=http://localhost:3000

# --- Storage ---
# "vercel-blob" or "r2"
STORAGE_PROVIDER=

# Vercel Blob (when STORAGE_PROVIDER=vercel-blob)
VERCEL_BLOB_READ_WRITE_TOKEN=

# Cloudflare R2 (when STORAGE_PROVIDER=r2)
CLOUDFLARE_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=

# --- AI Services ---
# Anthropic — Claude agent for listing generation
ANTHROPIC_API_KEY=

# OpenAI — Whisper for voice transcription
OPENAI_API_KEY=

# Google — Gemini for image enhancement
GEMINI_API_KEY=

# --- Background Jobs (Inngest) ---
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# --- Web Push Notifications (VAPID) ---
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
