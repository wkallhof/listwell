# ===========================================
# Listwell Admin Dashboard (apps/admin)
# ===========================================
# Copy to .env.local and fill in values

# API URL — where the Hono API is running (used for rewrites proxy + server-side fetches)
API_URL=http://localhost:4000

# Public app URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
